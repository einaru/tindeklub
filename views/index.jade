extends layouts/nobanner

block main
	main(role="main")
		article.home
			div
				.container
					h1 Tindeklub

					#slider.swipe
						.swipe-wrap
							for img in images
								div
									img(src=img.data)

					blockquote
						| I de svarte tinder
						br
						| finner den hvite snø
						br
						| evig hvile

					nav
						a(href="#") Journal
						|
						a(href="/gear") Gear
						|
						a(href="#") Beta

	include includes/footer

	script(src="//cdnjs.cloudflare.com/ajax/libs/zepto/1.0/zepto.min.js")
	script(src="//cdnjs.cloudflare.com/ajax/libs/swipe/2.0/swipe.min.js")
	script
		(function() {
			$('#slider').Swipe({
				auto: 5000,
				continuous: true
			}).data('Swipe');
		})(Zepto);
